---
alwaysApply: true
---

# 核心指令

1.  **优先复用现有组件**: 在实现任何功能之前，必须首先检查**[项目上下文]**中列出的“可复用模块/组件”。如果存在功能匹配或可适配的模块，必须优先调用或扩展它，而不是创建重复功能的代码。

2.  **依赖库使用规则**:
    *   **首要原则**: 所有代码实现，必须优先且严格地使用 **[项目上下文]** 的“已安装依赖库及版本”列表中指定的库和版本。
    *   **例外处理**: 如果经过分析，确认列表中现有的库无法满足任务需求，且必须引入新的外部库时，你必须遵循以下步骤：
        1.  **查找方案**: 优先使用 `exa-code` 工具，来查找实现该功能的最新、最主流的库及其实现方案（必须在当前项目中可用）。
        2.  **备选方案**: 如果 `exa-code` 未能提供合适的结果，则使用 `context7` 工具作为备选进行查找。
        3.  **明确报告与实现**: 在你的最终代码实现中，必须首先明确指出需要新增的依赖库名称（如果可能，也包括其最新稳定版本），然后再提供基于该新库的实现代码。